# Extracted scratch CSVs (re-extract from the committed .7z archives before running)
1-1_csv/
1-2_csv/
1-3_csv/
1-4_csv/
1-5_csv/
1-6_nano_correction_validation_real_video_0/1-6_csv/
2-1_csv/

# ad-hoc debug crops made while reviewing chart output
**/charts/zoom_*.png

# NOTE: 1-6_csv/v3_calibration/ (nested under the ignored 1-6_csv/ above)
# includes the v3 real-calibration pipeline's raw frame archive
# (frames_calib_eval_split.tar.gz, 3.1GB) and extracted frames/ -- both
# regenerable from generate_calib_eval_split_frames.py + frame_manifest.csv
# + the original Test_21/*.mp4 source videos (random.seed(7)/np.random.seed(7)).
# Kept only as local scratch; re-extract/re-run rather than expecting them
# to be committed once 1-6_csv.7z is repackaged.

# python bytecode cache
__pycache__/

# stray compile/log artifacts left in experiment folders (not repo-level
# build output, just leftover debug runs)
texput.log
compile_check.txt
